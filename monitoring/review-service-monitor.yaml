apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: review-service
  namespace: monitoring
  labels:
    monitoring: "true"
spec:
  selector:
    matchLabels:
      app: review-service
      monitoring: "true"
  namespaceSelector:
    matchNames:
    - team-git-happens
  endpoints:
    - port: http
      interval: 15s
      path: /actuator/prometheus
